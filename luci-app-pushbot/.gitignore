# 忽略指定的隐藏文件夹（末尾加 / 表示仅匹配文件夹）
.reasonix/

# 忽略 macOS 系统文件
.DS_Store

# Reasonix 本地配置
reasonix.toml

# 编译产物
*.ipk
*.apk

# 本地工具脚本
*.sh
# 本地备份/临时文件
*.bak
*.conf.bak
# 临时编译产物目录
.build-pkg/
# 构建时自动生成的出厂默认值备份（Makefile $(shell) 生成，单一源在 root/etc 与 root/usr/bin/pushbot/api）
root/usr/share/pushbot/defaults/
